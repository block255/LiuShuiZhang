# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Widget Preview related
.widget_preview/

# ────────────────────────────────────────────────────────────
# 本项目追加（2026-09-10）
# ────────────────────────────────────────────────────────────

# 密钥与口令：绝不入库（正式签名密钥本体在项目外的 签名密钥\ 目录）
*.jks
*.keystore
key.properties
**/key.properties

# 构建产物
*.apk
*.aab
android/.gradle/
android/.kotlin/
android/local.properties
# Gradle 在 android 模块下的输出（android/build、android/app/build）——
# 根目录的 /build/ 规则覆盖不到这里，必须显式排除
android/build/
android/app/build/

# 内部资料（若仓库建在 D:\Flutter_APP_Develop 这一层，这些一并排除；
# 若仓库就是 bill_record_app 本身，下面这些目录不存在，留着也无害）
打包区/
签名密钥/
测试文件库/
参考用样例文件/
笔记/
开发日志/
脚本库/
